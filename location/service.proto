syntax = "proto3";

package location;

service Location {
    rpc GetLocation(IpAddress) returns (Point);
}

message IpAddress {
    string address = 1;
}

message Point {
    double latitude = 1;
    double longitude = 2;
}
